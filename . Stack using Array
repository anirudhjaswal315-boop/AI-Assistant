#include <stdio.h>
#include <stdlib.h>

#define MAX 100

typedef struct Stack{ int top; int a[MAX]; } Stack;

void push(Stack *s,int v){ if(s->top==MAX-1){ printf("Overflow\n"); return; } s->a[++s->top]=v; }
int pop(Stack *s){ if(s->top==-1){ printf("Underflow\n"); return -1; } return s->a[s->top--]; }

int main(){
    Stack s; s.top=-1;
    push(&s,10); push(&s,20);
    printf("%d popped\n", pop(&s));
    printf("%d popped\n", pop(&s));
    return 0;
}
