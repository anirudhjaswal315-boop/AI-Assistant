#include <stdio.h>
#include <limits.h>

int main(){
    long long a,b; char op; scanf("%lld %c %lld",&a,&op,&b);
    if(op=='+'){ long long r=a+b; if((b>0 && a>LLONG_MAX-b) || (b<0 && a<LLONG_MIN-b)) printf("Overflow\n"); else printf("%lld\n",r); }
    else if(op=='-') printf("%lld\n", a-b);
    else if(op=='*') printf("%lld\n", a*b);
    else if(op=='/'){ if(b==0) printf("Div by zero\n"); else printf("%lld\n", a/b); }
    return 0;
}
