#include <stdio.h>
#include <string.h>

int main(){
    char a[512], b[512]; scanf("%s %s", a, b);
    int la=strlen(a), lb=strlen(b);
    int res[1024]={0};
    for(int i=la-1;i>=0;i--){
        for(int j=lb-1;j>=0;j--){
            res[(la-1-i)+(lb-1-j)] += (a[i]-'0')*(b[j]-'0');
        }
    }
    int n=la+lb;
    for(int i=0;i<n;i++){ int carry=res[i]/10; res[i]%=10; res[i+1]+=carry; }
    int i=n; while(i>0 && res[i]==0) i--;
    for(;i>=0;i--) printf("%d",res[i]); printf("\n");
    return 0;
}
