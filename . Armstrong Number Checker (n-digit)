#include <stdio.h>
#include <math.h>

int main(){
    int n; scanf("%d",&n);
    int temp=n, cnt=0;
    while(temp){ cnt++; temp/=10; }
    temp=n; int sum=0;
    while(temp){ int d=temp%10; sum += (int)pow(d,cnt); temp/=10; }
    printf(sum==n ? "Armstrong\n":"Not Armstrong\n");
    return 0;
}
