#include <stdio.h>

int main() {
    long long a = 0, b = 1, c;
    int n;  
    scanf("%d", &n);

    while(n--) {
        printf("%lld ", a);
        c = a + b;
        a = b;
        b = c;
    }
}
